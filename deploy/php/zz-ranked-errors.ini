; PHP diagnostics for production incident analysis
; Enable for debugging, then keep display_errors=Off in public environments.

log_errors = On
error_reporting = E_ALL
display_errors = Off

; Adjust if your distro uses another log path
error_log = /var/log/php/ranked-php-error.log
