controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

auth:
    path: /auth
    methods: ['POST']

gesdinet_jwt_refresh_token:
    path: /api/token/refresh
    controller: gesdinet.jwt_refresh_token.controller.refresh_token::refresh
    methods: ['POST']
