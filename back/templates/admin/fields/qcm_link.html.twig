{% if field.value %}
    <a href="{{ crud_url('QcmCrudController', 'detail', field.value.id) }}" class="text-decoration-none">
        {{ field.value }}
    </a>
{% else %}
    <em class="text-muted">Aucun QCM lié</em>
{% endif %}
