{% if field.value %}
    <a href="{{ crud_url('ContenuCrudController', 'detail', field.value.id) }}" class="text-decoration-none">
        {{ field.value }}
    </a>
{% else %}
    <em class="text-muted">Aucun contenu lié</em>
{% endif %}
