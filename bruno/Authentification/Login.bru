meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "eleve1@ranked.fr",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
