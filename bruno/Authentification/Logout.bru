meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "refresh_token": {{refreshToken}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
