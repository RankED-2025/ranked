meta {
  name: Me (User infos)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
