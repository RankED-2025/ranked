meta {
  name: Update my progression
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/progression/5
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "percentage": 90
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
