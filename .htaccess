<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteRule ^api/(.*)$ back/public/index.php [QSA,L]

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.html [L]
</IfModule>